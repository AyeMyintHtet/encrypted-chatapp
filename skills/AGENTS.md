When you create a ui, make sure it is responsive and accessible for current mobile and desktop users. for the colors use the theme.ts file to get the colors. For the icons used lucide react.And make sure icons, button, input box must have the "cursor-pointer" css.

For the Api Fetching or Supabase Fetching follow DRY principle. Don't call many apis if you can get the data from the previous api call or satify with just one api call.

for the fixes, when are you fixing something or adding new features or editing something and you found something is need to fix and if you have better idea to fix it, you can fix it. But if you are not sure about the fix, you can ask me first.

when you import something and you found the imported function that are didn't use in that page, you can delete without notice.

When you calling api make sure not to call two at the same time. We are stricted calling many apis at the same time.So make sure about it. 